[workspace]
members = [
    "battle_server",
    "data_grabbing",
    "burn_ai_model",
    "training",
    "preprocess",
]
resolver = "3"


[workspace.dependencies]
burn = "~0.19"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
burn_ai_model = { path = "./burn_ai_model" }
